export interface FormulaInfo {
  id: string;
  name: string;
  paper: string;
  description: string;
}

export function matchFormula(textContent: string): FormulaInfo | null {
  // Stub implementation
  return null;
}
