export interface FormulaInfo {
  id: string;
  name: string;
  paper: string;
  description: string;
}

export function matchFormula(text: string): FormulaInfo | null {
  // TODO: Restore actual formula matching logic. This file was found missing.
  return null;
}
