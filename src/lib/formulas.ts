export interface FormulaInfo {
  id: string;
  name: string;
  paper: string;
  description: string;
}

export function matchFormula(latex: string): FormulaInfo | null {
  // Stub implementation
  return null;
}
