export interface FormulaInfo {
  id: string;
  name: string;
  description: string;
  paper: string;
}

export function matchFormula(text: string): FormulaInfo | null {
  // TODO: Restore formula matching logic. File was missing.
  return null;
}
